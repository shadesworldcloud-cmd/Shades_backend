# -------------------------------------------------------
# Datasource
# -------------------------------------------------------
#spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/ECOMMERCE_DB?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true}
#spring.datasource.username=root
#spring.datasource.password=${DB_PASSWORD:}
#spring.datasource.password=0004
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}

# -------------------------------------------------------
# JPA / Hibernate
# -------------------------------------------------------
# Connection pool (HikariCP)
# -------------------------------------------------------
# Previously unset, which meant Hikari's default of 10 connections and NO acquisition timeout
# tuning — every request that could not get a connection simply waited 30s.
#
# Sizing formula, and why each number:
#   server capacity      : MySQL max_connections = 151 (measured, not assumed)
#   reserved             : 25 for migrations, the mysql CLI, monitoring and an admin session
#   available to apps    : 126
#   backend instances    : 1 (single instance — see DB_POOL_INSTANCES to re-derive if that changes)
#   ceiling per instance : 126
#
# The pool is NOT set to that ceiling. Throughput is bounded by cores, not by how many connections
# the server will tolerate: a pool larger than the database can service concurrently just moves
# queueing from the application into the database, where it is slower and harder to see. The
# working figure is the standard ((cores * 2) + effective spindles) ≈ 20 on this 24-core box, which
# sits far under the 126 ceiling and leaves headroom for a second instance without reconfiguration.
#
# connection-timeout is deliberately short. When the pool is exhausted the right behaviour is fast,
# visible backpressure — a controlled 500 the caller can retry — not a 30-second stall that ties up
# a request thread and makes the whole application look hung.
spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
spring.datasource.hikari.maximum-pool-size=${DB_POOL_MAX:20}
spring.datasource.hikari.minimum-idle=${DB_POOL_MIN_IDLE:5}
spring.datasource.hikari.connection-timeout=${DB_POOL_CONNECTION_TIMEOUT_MS:10000}
spring.datasource.hikari.validation-timeout=${DB_POOL_VALIDATION_TIMEOUT_MS:3000}
spring.datasource.hikari.idle-timeout=${DB_POOL_IDLE_TIMEOUT_MS:600000}
# Under MySQL's default wait_timeout (28800s) by a wide margin, and short enough that a connection
# broken by an intermediate proxy is retired before a request finds it.
spring.datasource.hikari.max-lifetime=${DB_POOL_MAX_LIFETIME_MS:1500000}
spring.datasource.hikari.keepalive-time=${DB_POOL_KEEPALIVE_MS:300000}
# Surfaces a connection held far longer than any legitimate transaction here — which is exactly the
# symptom of doing slow work (an SMTP send, an HTTP call) while holding one.
spring.datasource.hikari.leak-detection-threshold=${DB_POOL_LEAK_DETECTION_MS:30000}
spring.datasource.hikari.pool-name=ShadesWorldPool

# Bounds any transaction that would otherwise sit open indefinitely. Shorter than MySQL's
# innodb_lock_wait_timeout (50s) so the application gives up before the database does, and the
# resulting error is one we raise rather than a lock-wait surfacing from the driver.
spring.transaction.default-timeout=${DB_TX_TIMEOUT_SECONDS:30}

spring.jpa.hibernate.ddl-auto=validate
spring.jpa.open-in-view=false
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# -------------------------------------------------------
# JWT
# -------------------------------------------------------
security.jwt.secret=${JWT_SECRET:defaultDevSecretKeyThatIsAtLeast256BitsLongForHS256Algorithm!!}
security.jwt.access-token-expiration=${JWT_ACCESS_EXPIRATION:900000}
security.jwt.refresh-token-expiration=${JWT_REFRESH_EXPIRATION:604800000}
security.cookie.secure=${COOKIE_SECURE:false}
google.oauth.client-id=${GOOGLE_CLIENT_ID:654177765040-742kbr08fgphkafqnkigb84jnj78o2nj.apps.googleusercontent.com}

# -------------------------------------------------------
# CORS
# -------------------------------------------------------
app.cors.allowed-origins=${CORS_ORIGINS:http://localhost:3000,http://localhost:5173}

# -------------------------------------------------------
# Server
# -------------------------------------------------------
server.port=${SERVER_PORT:8080}
app.public-base-url=${PUBLIC_BASE_URL:http://localhost:8080}
app.frontend-base-url=${FRONTEND_BASE_URL:http://localhost:3000}
#app.upload.dir=${UPLOAD_DIR:uploads/products}
imagekit.private-key=${IMAGEKIT_PRIVATE_KEY}
imagekit.public-key=${IMAGEKIT_PUBLIC_KEY}
imagekit.url-endpoint=${IMAGEKIT_URL_ENDPOINT}

# Invoice seller identity. Replace these defaults with the business's legal details in production.
invoice.seller.name=${INVOICE_SELLER_NAME:Shades World}
invoice.seller.email=${INVOICE_SELLER_EMAIL:shadesworldindia11@gmail.com}
invoice.seller.address=${INVOICE_SELLER_ADDRESS:Shop 1, Plot 366, Lane 2, Opp Axis Bank, Raja Park, Jaipur, Rajasthan - 302004}
invoice.seller.contact=${INVOICE_SELLER_CONTACT:8233511042}
invoice.seller.gstin=${INVOICE_SELLER_GSTIN:08CFEPJ4650Q1ZX}
invoice.seller.pan=${INVOICE_SELLER_PAN:CFEPJ4650Q}
invoice.seller.state=${INVOICE_SELLER_STATE:Rajasthan - 08}
invoice.seller.website=${INVOICE_SELLER_WEBSITE:www.shadesworld.online}
invoice.product.hsn=${INVOICE_PRODUCT_HSN:90041000}
invoice.gst-rate=${INVOICE_GST_RATE:18.00}

# Password reset email (configure these environment variables; never commit real credentials)
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
#spring.mail.username=${MAIL_USERNAME:}
spring.mail.username=iambhuvanmod@gmail.com
#spring.mail.password=${MAIL_PASSWORD:}
spring.mail.password=hcgandlrmmettndj
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.checkserveridentity=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
app.email.outbox.enabled=${EMAIL_OUTBOX_ENABLED:true}

# Catalog
# How long a product carries the "New" badge, counted from PUBLISHED_AT in UTC. The boundary is
# exclusive: at exactly this many days the badge is gone. One value, read by NewProductPolicy —
# it used to be a 30 * 86400000 literal inside a browser-side mapping function.
app.catalog.new-product-days=${NEW_PRODUCT_DAYS:30}
# Images per product. A merchandising limit, not a technical one; enforced server-side so a caller
# bypassing the admin UI cannot exceed it.
# Per VARIANT: one main image plus up to nine additional. Photography belongs to variants now,
# so the old per-product ceiling would have capped a three-colour family at ten photos total.
app.catalog.max-variant-images=${MAX_VARIANT_IMAGES:10}
app.email.outbox.poll-delay-ms=${EMAIL_OUTBOX_POLL_DELAY_MS:2000}
app.email.outbox.max-attempts=${EMAIL_OUTBOX_MAX_ATTEMPTS:8}
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=25MB

# -------------------------------------------------------
# OpenAPI
# -------------------------------------------------------
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html

# Reservation window for unpaid orders. Stock is deducted when an order is created, so an
# abandoned checkout must have its stock released or those units are held forever.
app.orders.unpaid-expiry-minutes=${UNPAID_ORDER_EXPIRY_MINUTES:30}
app.orders.unpaid-expiry.enabled=${UNPAID_ORDER_EXPIRY_ENABLED:true}
app.orders.unpaid-expiry.poll-delay-ms=${UNPAID_ORDER_EXPIRY_POLL_MS:300000}
